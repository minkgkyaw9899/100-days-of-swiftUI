import Cocoa

var greeting = "Hello, playground"

//boolean data type

var fileName = "Mkk.png"

fileName.hasSuffix("png")

var gameOver = false

gameOver.toggle()

print(gameOver)

//join string together

let firstName = "Min"
let middlename = "Kaung"
let lastName = "Kyaw"

var name = firstName + middlename + lastName

name = firstName + "Kaung" + lastName

name = firstName + " " + middlename + " " + lastName

let age = 24;

print("My name is \(name) and age is \(age)")
